<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="gu.itu.skjapp.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        android:id="@+id/textView" />

    <Button
        android:text="Start addTripActivity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:id="@+id/start_add_trip_activity_btn"
        android:onClick=""/>

    <Button
        android:text="Start mapActivity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/add_new_member_activity_btn"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:id="@+id/start_map_activity_btn" />

    <Button
        android:text="Start addmemberActivity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/start_add_trip_activity_btn"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:id="@+id/add_new_member_activity_btn" />
</RelativeLayout>
